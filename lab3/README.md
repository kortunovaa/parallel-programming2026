
# Лабораторная работа №3
### Параллельное умножение матриц с использованием MPI

## Задание

Модифицировать программу из лабораторной работы №1 для параллельного выполнения с использованием технологии MPI.

**Требования:**
- Провести серию экспериментов с разным количеством процессов (1, 2, 4, 8)
- Провести эксперименты с разными размерами матриц (200, 400, 800, 1200, 1600, 2000)
- Исследовать влияние количества вычислительных ядер на производительность
- Выполнить автоматизированную верификацию результатов

## Реализация

В программу из лабораторной работы №1 были добавлены MPI функции для организации межпроцессного взаимодействия:

```cpp
int main(int argc, char* argv[]) {
    MPI_Init(&argc, &argv);

    int rank, num_processes;
    MPI_Comm_rank(MPI_COMM_WORLD, &rank);
    MPI_Comm_size(MPI_COMM_WORLD, &num_processes);

    // Только процесс 0 читает матрицы из файлов
    if (rank == 0) {
        MatrixA = Read_Matrix_From_File(matrixA_file);
        MatrixB = Read_Matrix_From_File(matrixB_file);
    }

    // Синхронизация процессов перед вычислениями
    MPI_Barrier(MPI_COMM_WORLD);
    
    // Процесс 0 выполняет умножение матриц
    if (rank == 0) {
        result = Multiply_Matrix_Sequential(MatrixA, MatrixB);
    }

    MPI_Finalize();
    return 0;
}

## Методика проведения экспериментов
1. Генерация матриц: Python-скрипт generate_matrix.py создает матрицы размеров 200, 400, 800, 1200, 1600, 2000
2. Количество потоков: 1, 2, 4, 8
3. Количество повторений: 3 замера для каждой комбинации (размер × потоки)
4. В x64 Native Tools Command Prompt for VS 2022 я прописала команды:
1) Перешла в правильную папку cd C:\Users\Настя\Desktop\параллельное программирование\lab3
2) Скомпилировала программу 
cl /EHsc /openmp Source.cpp /Fe:laba2_mpi.exe -I"C:\Program Files (x86)\Microsoft SDKs\MPI\Include" -link /LIBPATH:"C:\Program Files (x86)\Microsoft SDKs\MPI\Lib\x64" msmpi.lib (Если компиляция прошла успешно, появится файл laba2_mpi.exe)
3) Запускаю все тесты python test.py
4) После завершения
В папке появится файл experiments_mpi.csv с результатами

## Верификация
Все результаты экспериментов были автоматически проверены с помощью Python-скрипта paral.py:
Для каждой комбинации (размер × количество процессов) выполнена проверка корректности умножения
Сравнение с эталонным умножением через np.dot() из библиотеки NumPy
Проверка осуществлялась для всех 36 тестов (6 размеров × 4 варианта процессов × 3 повторения)
