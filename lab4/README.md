
# Лабораторная работа №4
### Параллельное умножение матриц с использованием CUDA

## Задание

Модифицировать программу из лабораторной работы №1 для параллельного выполнения с использованием технологии CUDA.

**Требования:**
- Провести серию экспериментов с разными конфигурациями блоков (8×8, 16×16, 32×32, 16×32, 32×16)
- Провести эксперименты с разными размерами матриц (200, 400, 800, 1200, 1600, 2000)
- Исследовать влияние размера блоков и сетки на производительность
- Выполнить автоматизированную верификацию результатов

## Реализация

В программу из лабораторной работы №1 были добавлены CUDA функции для организации параллельных вычислений на GPU:

```cpp
// Простое CUDA ядро (каждый поток вычисляет один элемент)
__global__ void matrixMulSimple(const double* A, const double* B, double* C, int n) {
    int row = blockIdx.y * blockDim.y + threadIdx.y;
    int col = blockIdx.x * blockDim.x + threadIdx.x;
    
    if (row < n && col < n) {
        double sum = 0.0;
        for (int k = 0; k < n; k++) {
            sum += A[row * n + k] * B[k * n + col];
        }
        C[row * n + col] = sum;
    }
}

// Оптимизированное ядро с использованием shared memory (для блоков 32×32)
__global__ void matrixMulOptimized(const double* A, const double* B, double* C, int n) {
    __shared__ double sharedA[32][32];
    __shared__ double sharedB[32][32];
    // ... блочная обработка с использованием shared memory
}
```
## Основные компоненты реализации:
Выделение памяти на GPU с помощью cudaMalloc
Копирование данных с хоста на устройство через cudaMemcpy
Запуск CUDA ядра с заданной конфигурацией блоков и сетки
Копирование результата обратно на хост
Автоматическая верификация через Python скрипт paral.py

## Методика проведения экспериментов
1. Генерация матриц: Python-скрипт generate_matrix.py создает матрицы размеров 200, 400, 800, 1200, 1600, 2000
2. Конфигурации блоков: 8×8, 16×16, 32×32, 16×32, 32×16
3. Количество повторений: 3 замера для каждой комбинации (размер × конфигурация блоков)
4. Среда выполнения: Google Colab с GPU Tesla T4
5. Команды для запуска:

```cpp
nvcc -o lab3_cuda Source.cu -lstdc++
python generate_matrix.py
python test.py
python create_table_cuda.py
```

## Верификация
Все результаты экспериментов были автоматически проверены с помощью Python-скрипта paral.py:
Для каждой комбинации (размер × конфигурация блоков) выполнена проверка корректности умножения
Сравнение с эталонным умножением через np.dot() из библиотеки NumPy
Проверка осуществлялась для всех 90 тестов (6 размеров × 5 конфигураций × 3 повторения)
Все проверки успешно пройдены (Result: CORRECT для каждого теста)
Корректность CUDA реализации подтверждена

## сводная таблица результатов
### Анализ полученных результатов
200*200
| Конфигурация | Среднее время(мс) | Производительность(GFLOPS) |
|:--------------:|:-------:|:--------:|
| 8*8 | 0.339 | 47.46|
| 16*16 | 0.347 | 46.44 |
| 32*32 | 0.478 | 33.59 |
| 16*32 | 0.341 | 47.31 |
| 32*16 | 0.408 | 39.42 |

400*400
| Конфигурация | Среднее время(мс) | Производительность(GFLOPS) |
|:--------------:|:-------:|:--------:|
| 8*8 | 0.974 | 131.51|
| 16*16 | 0.978 | 131.31 |
| 32*32 | 1.008 | 127.19 |
| 16*32 | 1.182 | 111.85 |
| 32*16 | 1.411 | 92.95 |

800*800
| Конфигурация | Среднее время(мс) | Производительность(GFLOPS) |
|:--------------:|:-------:|:--------:|
| 8*8 | 11.6580 | 87.83|
| 16*16 | 7.165 | 143.14 |
| 32*32 | 9.355 | 113.46 |
| 16*32 | 10.191 | 101.73 |
| 32*16 | 10.623 | 96.27 |

Приложение: использованное оборудование
GPU: NVIDIA Tesla T4 (15360 MB VRAM)
CUDA Version: 13.0
Driver Version: 580.82.07
Среда выполнения: Google Colab

# Вывод
**1. Программа из лабораторной работы №1 успешно модифицирована для параллельной работы с использованием технологии CUDA**

**2. Корректность результатов подтверждена автоматической верификацией (Result: CORRECT для всех 90 экспериментов)**

**3. Анализ производительности показал:**
Для маленьких матриц (200×200) эффективны маленькие блоки (8×8)
Для средних и больших матриц оптимальны блоки 16×16
Достигнута производительность 143.14 GFLOPS, что составляет 57% от теоретического пика Tesla T4
Производительность растет с увеличением размера матрицы (ускорение в 3× при переходе от 200 к 800)

**4. Выявлены особенности CUDA оптимизации:**
Размер матрицы, кратный размеру блока, позволяет использовать оптимизированное ядро с shared memory
Это дает дополнительный прирост производительности для матриц 400, 800, 1200, 1600, 2000

